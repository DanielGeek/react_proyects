const nombre = 'Boris';
const apellido = 'Alarcon';

const n1 = 1;
const n2 = 2;
// const imprimeNs = n1 + ' + ' + n2;
const imprimeNs = `${n1} + ${n2}`;

// const nombreCompleto = nombre + ' ' + apellido;
const nombreCompleto = `${ nombre } ${ apellido }`;
console.log(nombreCompleto);
console.log(imprimeNs);